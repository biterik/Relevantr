name: relevantr
channels:
  - conda-forge
dependencies:
  - python=3.11
  - tk  # Tkinter GUI toolkit
  - pip
  - pip:
      # Installs Relevantr with the local embedding backend and the
      # cross-encoder reranker (see pyproject.toml for the dependency list).
      - -e .[local,test]
